"""Provider plugins.

Each provider knows how to identify, normalize and evaluate URLs for one
platform. Providers are registered in :mod:`blhawk.providers.registry`.
"""
from __future__ import annotations
